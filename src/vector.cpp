#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

class Solution{
public:

    int GetMatchedWordCount(const vector<vector<char>>& charMatrix, const vector<string> words)
    {
        if(charMatrix.empty() || charMatrix[0].empty()) {
            return 0;
        }

        vector<bool> memo(words.size(), false);
        for(int i=0)

        return 0;
    }

};



int main()
{
    return 0;
}